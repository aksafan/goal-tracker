// import { prisma } from "@/db/prisma";
// import { GoalProgressModel } from "@/goal/goal.types";

export default class GoalProgressService {
  // findAll = async (goalId: string, userId: string) => {
  //   await prisma.goalProgress.findMany({
  //     where: { goal_id: goalId, user_id: userId },
  //   });
  // }
  //
  // create = async (goalId: string, data: any, userId: string) => {
  //   await prisma.goalProgress.create({
  //     data: {
  //       goal_id: goalId,
  //       user_id: userId,
  //       goal_type_field_id: data.goal_type_field_id,
  //       progress_value: data.progress_value,
  //     },
  //   });
  // }
}
